{{- if .Values.projects.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-projects
  labels:
    {{- include "consulting-pm.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-projects
  minReplicas: {{ .Values.projects.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.projects.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.projects.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
