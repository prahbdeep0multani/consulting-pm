apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-billing
  labels:
    {{- include "consulting-pm.labels" . | nindent 4 }}
spec:
  selector:
    app: billing
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: {{ .Values.billing.service.port }}
      targetPort: {{ .Values.billing.service.port }}
