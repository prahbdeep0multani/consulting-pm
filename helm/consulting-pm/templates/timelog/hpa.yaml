{{- if .Values.timelog.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-timelog
  labels:
    {{- include "consulting-pm.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-timelog
  minReplicas: {{ .Values.timelog.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.timelog.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.timelog.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
